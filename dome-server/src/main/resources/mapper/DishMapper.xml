<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd" >
<mapper namespace="com.yurirvs.dome.mapper.DishMapper">

    <insert id="addDish" useGeneratedKeys="true" keyProperty="id">
        INSERT INTO dish
        VALUES (null, #{name}, #{categoryId}, #{price}, #{image}, #{description}, #{status}, #{createTime},
                #{updateTime}, #{createUser}, #{updateUser})
    </insert>

    <select id="pageQuery" resultType="com.yurirvs.dome.vo.DishVO">
        SELECT d.*, c.`name` category_name
        FROM dish d
                 LEFT OUTER JOIN category c ON d.category_id = c.id
        <where>
            <if test="name != null">
                AND d.name LIKE concat('%',#{name},'%')
            </if>
            <if test="categoryId != null">
                AND d.category_id LIKE concat('%',#{categoryId},'%')
            </if>
            <if test="status != null">
                AND d.status LIKE concat('%',#{status},'%')
            </if>
        </where>
    </select>

    <delete id="deleteByIds">
        DELETE
        FROM dish
        WHERE id IN
        <foreach collection="ids" item="id" open="(" close=")" separator=",">
            #{id}
        </foreach>
    </delete>

    <update id="updateById">
        UPDATE dish
        <set>
            <if test="name != null and name != ''">
                name = #{name},
            </if>
            <if test="categoryId != null">
                category_id = #{categoryId},
            </if>
            <if test="price != null">
                price = #{price},
            </if>
            <if test="image != null and image != ''">
                image = #{image},
            </if>
            <if test="description != null">
                description = #{description},
            </if>
            <if test="status != null">
                status = #{status},
            </if>
            <if test="updateTime != null">
                update_time = #{updateTime},
            </if>
            <if test="updateUser != null">
                update_user = #{updateUser},
            </if>
        </set>
        WHERE id = #{id}
    </update>

    <select id="getBySetmealId" resultType="com.yurirvs.dome.entity.Dish">
        SELECT d.*
        FROM dish d
                 INNER JOIN setmeal_dish sd ON d.id = sd.dish_id
        WHERE sd.setmeal_id = #{SetmealId}
    </select>

    <select id="list" resultType="com.yurirvs.dome.entity.Dish">
        SELECT *
        FROM dish
        <where>
            <if test="id != null">
                AND id = #{id}
            </if>

            <if test="name != null and name != ''">
                AND name LIKE CONCAT('%', #{name}, '%')
            </if>

            <if test="categoryId != null">
                AND category_id = #{categoryId}
            </if>

            <if test="price != null">
                AND price = #{price}
            </if>

            <if test="status != null">
                AND status = #{status}
            </if>

            <if test="description != null and description != ''">
                AND description LIKE CONCAT('%', #{description}, '%')
            </if>

            <if test="createUser != null">
                AND create_user = #{createUser}
            </if>

            <if test="updateUser != null">
                AND update_user = #{updateUser}
            </if>
        </where>
    </select>

    <select id="countByMap" resultType="java.lang.Integer">
        select count(id) from dish
        <where>
            <if test="status != null">
                and status = #{status}
            </if>
            <if test="categoryId != null">
                and category_id = #{categoryId}
            </if>
        </where>
    </select>
</mapper>
