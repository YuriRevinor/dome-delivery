<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd" >
<mapper namespace="com.yurirvs.dome.mapper.ShoppingCartMapper">

    <select id="list" resultType="com.yurirvs.dome.entity.ShoppingCart">
        SELECT *
        FROM shopping_cart
        <where>
            <if test="id != null">
                AND id = #{id}
            </if>
            <if test="name != null and name != ''">
                AND name = #{name}
            </if>
            <if test="userId != null">
                AND user_id = #{userId}
            </if>
            <if test="dishId != null">
                AND dish_id = #{dishId}
            </if>
            <if test="setmealId != null">
                AND setmeal_id = #{setmealId}
            </if>
            <if test="dishFlavor != null and dishFlavor != ''">
                AND dish_flavor = #{dishFlavor}
            </if>
            <if test="number != null">
                AND number = #{number}
            </if>
            <if test="amount != null">
                AND amount = #{amount}
            </if>
            <if test="image != null and image != ''">
                AND image = #{image}
            </if>
            <if test="createTime != null">
                AND create_time = #{createTime}
            </if>
        </where>
    </select>

    <insert id="insert" parameterType="com.yurirvs.dome.entity.ShoppingCart"
            useGeneratedKeys="true" keyProperty="id">
        INSERT INTO shopping_cart (
            name,
            user_id,
            dish_id,
            setmeal_id,
            dish_flavor,
            number,
            amount,
            image,
            create_time
        )
        VALUES (
                   #{name},
                   #{userId},
                   #{dishId},
                   #{setmealId},
                   #{dishFlavor},
                   #{number},
                   #{amount},
                   #{image},
                   #{createTime}
               )
    </insert>
</mapper>
